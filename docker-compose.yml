services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: opsdesk
      POSTGRES_PASSWORD: opsdesk
      POSTGRES_DB: opsdesk
    ports:
      - "5432:5432"
    volumes:
      - opsdesk_db:/var/lib/postgresql/data

volumes:
  opsdesk_db:

